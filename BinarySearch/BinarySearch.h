#include <iostream>
#include <vector>
class BinarySearch{
    public:
    //has to be sorted
    //if null return -1
    static int Search(std::vector<int*>& arr, int target, int start, int end){
        // if(start < 0) start = 0;
        // if(start > arr.size()) start = arr.size();
        // if(*arr[start] < target) return Search(arr, target, (start - end) / 2, end - 1);
        // else if(*arr[start] > target) return Search(arr, target, (start - end) / 2, end - 1);
        // else if(*arr[start] == target) return *arr[start];
        // else return -1;
        return 0;
    }
};