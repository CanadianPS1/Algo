#include <iostream>
#include <vector>
/*
For Loop
    For Loop
        check if the place is bigger then the next
            swap
        else 
            skip
OC = O(n ^2)
SC = O(1)
*/
template <typename T>
class SelectionSort{
    public:
    std::vector<T> Sort(std::vector<T> arr){
        
    }
};